# Legacy PHP installer DB config (contains credentials) — never track.
/Data.php

# Source drop for background art (runtime copies live in frontend/public/bg/).
/animated bg/

# HCW-SMS React/Mantine UI — deps and build artifacts (run `npm run build`).
/frontend/node_modules/
/frontend/dist/

# Rust build output (standalone server + Tauri) and Tauri codegen.
/server/target/
/src-tauri/target/
/src-tauri/gen/

# Bundled sidecar binary — regenerated at build time by scripts/prepare-build.mjs.
/src-tauri/binaries/

# Local SQLite DB + portable school files created by the Rust server.
*.sqlite
*.sqlite-*
*.leosdb

# Test harness — deps, runner artifacts, reports, and the temp server-info file.
/node_modules/
/test-results/
/tests/reports/
/tests/.tmp/
/playwright-report/

# Tooling / local
/.claude/

# OS / editor noise
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store
*.swp
*~
.vscode/
.idea/
