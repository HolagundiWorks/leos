# HCW-SMS DB connection config written by the installer — contains credentials.
# Keep it out of version control; the installer regenerates it.
/Data.php

# HCW-SMS React/Mantine UI — deps and build artifacts (run `npm run build`).
/frontend/node_modules/
/frontend/dist/
/assets/hcw-ui/

# Local SQLite DB + portable school files created by the Rust server.
*.sqlite
*.sqlite-*
*.schoolpkg

# OS / editor noise
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store
*.swp
*~
.vscode/
.idea/
